# AprilTag 工况与场景管理中枢 (Scene Hub) 技术与操作指南

> **文档版本**: v2.1 (2026-09)  
> **适用模块**: `tools/scene_hub.py` (`tools/scene_hub/`) & `src/calibration/scene_manager.py`  
> **系统环境**: Windows 10/11 x64, Python 3.11+, OpenCV 4.x

---

## 1. 系统概述与设计哲学

在工业 3D 视觉与多 AprilTag 空间标定任务中，工程现场面临多视角采样批次繁多、不同工况/光照条件数据混乱、平差前缺乏直观质量体检，以及**测试标定参数与生产系统运行地图界限不清**等痛点。

**工况与场景管理中枢 (Tag Scene Hub)** 采用 1280x720 高清工业控制台架构，核心定位为**“工况管理与数据工作空间 (Data & Workspace Management Hub)”**：
- **场景即工作空间**：场景是样本图像 `raw_images/`、元数据 `scene_meta.yaml`、空间平差地图 `tags_map.yaml` 与质检报告 `reports/` 的数据容器，与底层相机硬件取流解耦；
- **专职采图工具委托**：Scene Hub 自身不维持相机取流循环，彻底避免占用 USB 硬件句柄。按 `[C]` 键即可秒级拉起专职采图向导 `capture_wizard.py`，采图完毕后自动平滑重新装载样本与刷新状态；
- **全链路闭环流转**：无缝连接“**工况沙盒管理 $\rightarrow$ 采图向导抓拍 $\rightarrow$ 几何健康体检 $\rightarrow$ 离线平差求解 $\rightarrow$ 留一盲测验证 $\rightarrow$ 一键原子生效生产系统**”。

---

## 2. 核心架构与【标定工况沙盒 vs 生产基准】机制

系统严格遵循**“工况沙盒隔离、平差质检验证、生产原子发布”**的工业安全原则：

```mermaid
flowchart LR
    A[标定工况场景沙盒] -->|C 采图 / S 平差| A
    A -->|P 生效到生产| B[★ 生产运行地图 (config/tags_map.yaml)]
    B -->|在线主程序读取| C[3D 视觉测量与定位生产环境]
```

### 2.1 状态与机制定义

1. **标定工况场景 (Calibration Scene)**:
   - 本地独立文件夹保存原始图像与体检报告；多工况完全平权并行；
   - 处于独立沙盒环境，修改、采图、平差求解绝不影响车间流水线生产；
   - 任意场景只要完成平差解算，卡片上均可按 `[P]` 或点击直接发布。
2. **生产运行基准 (Production Benchmark)**:
   - 真正被系统生产管道（`config/tags_map.yaml`）加载的现场唯一真值地图；
   - 顶部 Header 中间醒目展示：`生产运行地图: 【XXX】`；
   - 对应的场景卡片右下角将获得金色荣誉徽章 `★ 生产运行`。

---

## 3. 界面布局与三模态视图指引

驾驶舱采用 1280x720 工业控制台深色科技风布局，支持三种无缝切换的视图模式：

```
+---------------------------------------------------------------------------------------------------+
| flux_vision_3d | 工况与场景管理中枢 (Scene Hub)   [⊞标准|⤢大图|▤看板]   生产运行: 【现场基准】  [M]工具箱 [H]说明 [X]退出 |
+-------------------+-----------------------------------+-------------------------------------------+
| 工况场景批次 (5)    | 场景综合体检与几何健康报告         | 采样相册缩略图与单帧大图视口                |
| 01. 现场实测 [★生产]|  - 基础指标卡片 (图像/Tag/RMSE)    |  - 多视角缩略图网格                        |
|     [P 生效生产]   |  - 几何网络健康度 (环路/跨度)      |  - 快捷按键 [< / >] 滚轮翻页               |
| 02. 对照组A        |  - 工业质检放行仪表盘              |  - 单帧大图全宽自适应 [F]                  |
|     [P 生效生产]   |                                   |  (纯净看板模式下相册关闭，右侧940全景舒展) |
| ----------------- |                                   |                                           |
| 场景快捷操作:     |                                   |                                           |
| [N]新建  [R]改名  |                                   |                                           |
| [K]克隆  [V]目录  |                                   |                                           |
| [C]相机连拍采图   |                                   |                                           |
+-------------------+-----------------------------------+-------------------------------------------+
| [↑/↓]选场景 [⏎]设为活动 [P]生效生产 [C]连拍 [S]平差 [F]切换视图              SANDBOX: ★ 生产运行基准  |
+---------------------------------------------------------------------------------------------------+
```

### 3.1 三模态视图切换机制
- **模式 1：标准三栏视图 (`[ ⊞ 标准 ]`)**：左 340 场景卡片 + 中 460 体检健康报告 + 右 480 采样相册与大图；
- **模式 2：全宽大图沉浸视图 (`[ ⤢ 大图 ]`)**：左 340 场景卡片 + 右 940 大图自适应铺满全屏，用于巡检角点定位精度与高光反光；
- **模式 3：纯净体检健康大屏 (`[ ▤ 看板 ]`)**：左 340 场景卡片**绝对固定不动** + 右 940 宽松舒展展示**场景核心体检、几何网络健康度与 0~29 号 Tag 拓扑覆盖矩阵**，采样相册完全关闭隐藏，杜绝多余图形干扰；
- **切换方式**：鼠标点击顶部三段式 Tab 胶囊，或按键盘 **`[F]`** 键单键顺次循环。

### 3.2 顶部标题栏 (Header, 0~50px)
- **三段式视图 Tab 胶囊**：直观高亮指示当前所处模式，点击一秒直达；
- **生产运行地图信息**：居中展示当前实际在线的地图名称与物理 ID，点击可秒级唤出业务机制解析看板；
- **[M] 工具箱按钮**：点击唤出 6 合 1 独立标定工具总菜单；
- **[H] 生产说明按钮**：点击唤出【活动 vs 生产】黄金闭环全景流程弹窗；
- **[X] 退出按钮**：深红科技警戒按钮，支持**鼠标直接点击**或按 **[ESC]** 安全退出。

### 3.2 左侧导航栏 (Left Panel, 0~340px)
- **场景批次卡片列表**：单屏可容纳 5 个工况卡片，支持鼠标滚轮与方向键自由翻页；
  - **活动卡片**：展示 `● 活动中` 绿色徽章，并拥有专属的高亮 `[P 生效生产]` 按钮；
  - **草稿卡片**：展示 `[设为活动 ⏎]` 胶囊按钮，**鼠标点击或按回车键立即将其切换为当前活动沙盒**；
- **鼠标右键快捷上下文菜单 (Context Menu)**：
  - 在任意场景卡片上**点击鼠标右键**，即在光标处唤起专属深色科技浮层菜单；
  - 针对该条目的操作全部收敛进右键菜单中：
    1. **生效到生产系统**（快捷键 `[P]`，仅已完成平差场景可用）
    2. **重命名场景别名**（快捷键 `[R]`）
    3. **克隆当前场景副本**（快捷键 `[K]`）
    4. **打开该场景物理目录**（快捷键 `[O]`）
    5. **安全删除场景**（快捷键 `[DEL]`，生产中场景受保护禁止误删）
  - 支持鼠标 Hover 高亮，点击菜单外空白处或按 `[ESC]` 安全关闭。
- **左下方全局级操作按钮**：按用户工效规范精炼保留核心全局功能：
  - `[+] 新建工况 [N]`：轻量级弹窗输入中文工况别名；
  - `[V] 场景总目录`：在 Windows 资源管理器中打开所有场景存放的总物理文件夹；
  - `[C] 相机连拍向导`：全宽醒目按钮，一键切入相机取流与连续抓拍。

### 3.3 中间报告栏 (Center Panel, 340~800px)
- **核心元数据卡片**：显示中文友好别名、物理 ASCII 目录 ID、样本帧数、求解状态；
- **基础体检指标**：全量样本检测标签总数、覆盖唯一 Tag 数量、全局 RMSE 重投影误差；
- **空间几何网络健康度**：跨视角最大基线跨度 (mm)、平均单帧标签冗余度、闭环观察数；
- **质检放行建议**：智能评估当前场景是否满足工业高精生产要求（RMSE < 0.8px 且几何健康）。

### 3.4 右侧相册栏 (Right Panel, 800~1280px)
- **缩略图网格**：展示当前场景已采样的每张视角图纸及检测到的 AprilTag 数量角标；
- **大图全宽预览 [F]**：按 `F` 键瞬间将中间与右侧栏合体为全宽高分辨率视口，利用滚轮顺畅巡检单帧角点检测品质。

### 3.5 底部状态栏 (Footer, 670~720px)
- **左侧操作提示**：实时反馈当前系统状态与 Toast 提示；
- **右侧 Sandbox 隔离状态胶囊**（彻底解耦相机流，直观指示当前场景发布状态）：
  - **生产基准状态**：金黄色常亮徽章，显示 `SANDBOX: ★ 生产运行基准`；
  - **已平差就绪状态**：亮绿色呼吸灯，显示 `SANDBOX: 已平差 (RMSE: X.XXX px)`；
  - **草稿沙盒状态**：灰蓝色沉稳胶囊，显示 `SANDBOX: 草稿沙盒`。

---

## 4. 黄金闭环 3 步走 SOP 操作规范

生产现场推荐标准操作流程：

1. **Step 1: 新建或选定草稿场景**
   - 按 `[N]` 新建场景（输入中文别名，如 `20260915_车间产线标定`）；
   - 或选中历史场景，点击卡片右上角 `[设为活动 ⏎]` 将其设为活动沙盒。
2. **Step 2: 专职采图向导与深度平差**
   - 点击 `[C] 采图向导` 或按键盘 `[C]` 键，自动调出专用采图向导 (`capture_wizard.py`)；
   - 在采图向导多视角多高度引导下，按 `[Space]` 空格抓拍 10~20 帧高质样本；
   - 退出向导后 Scene Hub 自动刷新装载最新样本，按 `[S]` 调出 Studio 平差工作站剔除粗差求解 3D 坐标；
   - 观察中间栏体检报告：确认 RMSE 重投影误差优于 0.8px 且几何网络健康度达标。
3. **Step 3: 严格质检后一键生效到生产**
   - 点击当前活动卡片上的专属 `[P] 生效生产` 按钮（或直接按键盘 `[P]` 键）；
   - 系统原子更新生产配置文件 `config/tags_map.yaml`，并建立时间戳安全备份；
   - 场景状态立即升级为 `★ 生产运行`，完成现场标定闭环！

---

## 5. 性能设计与高频交互优化

针对高频鼠标 Hover 与海量 YAML 数据处理，Scene Hub 实现了全链路极致优化：
- **帧级命中区碰撞缓存 (Hit-box Cache - 248,000 FPS)**：
  - 传统方案只要鼠标产生微小位移（如每秒数十次微移动）即触发全屏重新渲染（包括所有卡片阴影、渐变与几十处中文文字光栅化）；
  - Scene Hub 创新设计 `_get_interactive_hover_key()`，精准判断当前光标是否处于任何交互热区（卡片、按钮、上下文菜单等）。若光标在空白区域移动或维持在同一按钮内，直接零拷贝返回已缓存的双缓冲画布；
  - Hover 响应时间从 ~13s 骤降至 **0.004 ms (248,000 FPS)**，彻底根治鼠标移动画面卡死冻结问题。
- **元数据惰性反序列化与多级缓存 (0ms 场景读取)**：
  - 传统模式每次渲染单帧都会穿透调用 `CalibrationScene.load()`，进而调用 `refresh_stats()`，使用纯 Python `yaml.safe_load` 反复从磁盘解析 70KB 观测数据 YAML，导致单次加载耗时 8~13 秒；
  - 优化后 `CalibrationScene.load()` 默认优先信赖 `scene_meta.yaml` 中的持久化状态；`CalibrationSceneManager` 建立内部场景内存缓存；`HubState` 直接从内存场景列表索引，场景数据读取实现 **0ms / 41ms 极速响应**（提速超 200 倍）。
- **微切片 Patch 局部贴图 (<1.2ms)**：
  - 非 ASCII 中文文本绘制基于 Bounding Box 进行轻量微切片贴图，内存操作量从 2.7MB 降低至 10KB。
- **Unicode / 中文路径安全兼容**：
  - 底层基于 `cv2.imdecode` 与 `cv2.imencode` 封装 `imread_unicode` 与 `imwrite_unicode`，杜绝 Windows 平台中文路径黑屏与加载失败。

## 6. 世界坐标系锚点与 anchor scale factor (锚点尺度因子)

为消除单一 Tag 边长 (50mm) 测量误差 (~0.5mm 即引入 1% 尺度漂移) 对全局空间精度的影响，Scene Hub 在【生效到生产】阶段引入绝对坐标系锚定与尺度因子同步机制：

### 6.1 绝对锚点定义 (与 `config.yaml` `calibration.world_anchor` 同源)

| 锚点 | 标靶 ID | 绝对坐标 (机械臂坐标系, mm) | 角色 |
| :---: | :---: | :---: | :--- |
| 原点锚点 | Tag 0 | $(0, 0, 405)$ | 平移原点 + $Z$ 轴方向参照 |
| 对齐锚点 | Tag 1 | $(0, 520, 196)$ | $X$ 轴方向参照 + 尺度基线 |

### 6.2 anchor scale factor 解算流程

1. **BA 平差阶段保持像素为单位**：避免在优化中混淆像素与物理量纲；
2. **平差收敛后引入绝对坐标**：计算 Tag 0 与 Tag 1 在 BA 解算地图中的标称距离 $D_{nominal}$，与绝对坐标物理距离 $D_{real} = \sqrt{0^2 + 520^2 + (405-196)^2} \approx 560.5\text{mm}$ 比对；
3. **尺度锁定**：$\text{anchor\_scale} = D_{real} / D_{nominal}$，作为全局等比缩放因子；
4. **marker 尺寸模型同步**：`real_marker_size = nominal_marker_size × anchor_scale` ( nominal = 50mm )，所有 Tag 位姿统一表达在以 Tag 0 为原点、Tag 0→Tag 1 为 $X$ 轴、Tag 0 法向为 $Z$ 轴的世界坐标系下。

### 6.3 与生产生效的衔接

`[P] 生效生产` 按钮触发时，原子写入 `config/tags_map.yaml` 的地图同时包含：
- 全部 Tag 的世界坐标系位姿 (已乘 anchor_scale)；
- `anchor_scale` 字段 (供下游 `tag_localizer.py` 在线解算时复用)；
- `world_anchor` 元信息 (origin/align tag id + 绝对 XYZ)。

下游生产程序 (芦笋抓取、Robot 在线跟踪) 读取地图时，统一基于该世界坐标系解算目标位姿，与机械臂绝对坐标 `(X0, Y600, Z80, R90°)` 直接对齐。
